#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    for (int i = 0; str[i]; i++)
        if (isdigit(str[i]))
            str[i] = '#';
    printf("After replacement: %s\n", str);
    return 0;
}
