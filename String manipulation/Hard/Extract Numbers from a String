#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {
    char str[100];
    printf("Enter string with numbers: ");
    gets(str);

    printf("Numbers found: ");
    for (int i = 0; str[i]; i++)
        if (isdigit(str[i])) printf("%c", str[i]);

    return 0;
}
