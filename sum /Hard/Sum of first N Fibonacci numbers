#include <stdio.h>
int main() {
    int n, a=0, b=1, c, sum=1;
    printf("Enter n: ");
    scanf("%d", &n);
    for(int i=3; i<=n; i++){
        c = a + b;
        sum += c;
        a = b;
        b = c;
    }
    printf("Sum of Fibonacci = %d", sum);
    return 0;
}
