#include <stdio.h>
int main() {
    int n, i;
    long long num = 0, sum = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(i = 1; i <= n; i++) {
        num = num * 10 + 1;
        sum += num;
    }
    printf("Sum = %lld", sum);
    return 0;
}
