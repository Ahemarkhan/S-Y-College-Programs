#include <stdio.h>
int main() {
    int n, d, sum = 0;
    printf("Enter number: ");
    scanf("%d", &n);
    while(n > 0) {
        d = n % 10;
        if(d % 2 != 0)
            sum += d;
        n /= 10;
    }
    printf("Sum of odd digits = %d", sum);
    return 0;
}
