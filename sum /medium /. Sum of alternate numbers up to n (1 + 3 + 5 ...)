#include <stdio.h>
int main() {
    int n, sum = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(int i = 1; i <= n; i += 2)
        sum += i;
    printf("Sum of alternate numbers = %d", sum);
    return 0;
}
