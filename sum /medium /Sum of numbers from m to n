#include <stdio.h>
int main() {
    int m, n, sum = 0;
    printf("Enter start and end values: ");
    scanf("%d%d", &m, &n);
    for(int i = m; i <= n; i++)
        sum += i;
    printf("Sum = %d", sum);
    return 0;
}
