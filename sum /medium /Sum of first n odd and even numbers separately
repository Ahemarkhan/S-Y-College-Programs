#include <stdio.h>
int main() {
    int n, oddSum = 0, evenSum = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        if(i % 2 == 0) evenSum += i;
        else oddSum += i;
    }
    printf("Even Sum = %d, Odd Sum = %d", evenSum, oddSum);
    return 0;
}
