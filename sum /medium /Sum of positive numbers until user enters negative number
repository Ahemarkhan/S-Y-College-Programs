#include <stdio.h>
int main() {
    int num, sum = 0;
    printf("Enter numbers (negative to stop): ");
    while(1) {
        scanf("%d", &num);
        if(num < 0)
            break;
        sum += num;
    }
    printf("Sum = %d", sum);
    return 0;
}
