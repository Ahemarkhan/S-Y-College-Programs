#include <stdio.h>
int main() {
    int a, b, sum;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    sum = a + b;

    if(sum % 2 == 0)
        printf("Sum %d is Even", sum);
    else
        printf("Sum %d is Odd", sum);

    return 0;
}
