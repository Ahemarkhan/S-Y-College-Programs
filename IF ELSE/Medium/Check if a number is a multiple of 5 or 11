#include <stdio.h>
int main() {
    int n;
    printf("Enter number: ");
    scanf("%d", &n);

    if(n % 5 == 0 && n % 11 == 0)
        printf("Multiple of both 5 and 11");
    else if(n % 5 == 0)
        printf("Multiple of 5 only");
    else if(n % 11 == 0)
        printf("Multiple of 11 only");
    else
        printf("Not a multiple of 5 or 11");
    return 0;
}
