
#include <stdio.h>
int main() {
    int m,p,c;
    printf("Enter marks in Maths, Physics, Chemistry: ");
    scanf("%d%d%d", &m,&p,&c);

    if(m >= 65 && p >= 55 && c >= 50 && (m+p+c) >= 190)
        printf("Eligible for admission");
    else
        printf("Not eligible");
    return 0;
}
