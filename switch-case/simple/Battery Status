#include <stdio.h>
int main() {
    int level;
    printf("Enter battery level (1=Low, 2=Medium, 3=Full): ");
    scanf("%d", &level);
    switch(level) {
        case 1: printf("Battery Low - Charge soon.\n"); break;
        case 2: printf("Battery Medium - Okay.\n"); break;
        case 3: printf("Battery Full - Good.\n"); break;
        default: printf("Invalid level.\n");
    }
    return 0;
}
🕒 6. Time of Day
c
Copy code
#include <stdio.h>
int main() {
    int hour;
    printf("Enter hour (0–23): ");
    scanf("%d", &hour);
    switch(hour / 6) {
        case 0: printf("Good Night\n"); break;
        case 1: printf("Good Morning\n"); break;
        case 2: printf("Good Afternoon\n"); break;
        case 3: printf("Good Evening\n"); break;
        default: printf("Invalid hour\n");
    }
    return 0;
}
📱 7. Mobile Mode
c
Copy code
#include <stdio.h>
int main() {
    int mode;
    printf("Enter mode (1=Silent, 2=Vibrate, 3=Ringing): ");
    scanf("%d", &mode);
    switch(mode) {
        case 1: printf("Phone is in Silent Mode.\n"); break;
        case 2: printf("Phone is in Vibrate Mode.\n"); break;
        case 3: printf("Phone is Ringing.\n"); break;
        default: printf("Invalid mode.\n");
    }
    return 0;
}
🧾 8. Grade System
c
Copy code
#include <stdio.h>
int main() {
    char grade;
    printf("Enter grade (A, B, C, D, F): ");
    scanf(" %c", &grade);
    switch(grade) {
        case 'A': printf("Excellent!\n"); break;
        case 'B': printf("Good Job.\n"); break;
        case 'C': printf("Well Done.\n"); break;
        case 'D': printf("You Passed.\n"); break;
        case 'F': printf("Better Try Again.\n"); break;
        default: printf("Invalid Grade.\n");
    }
    return 0;
}
🧊 9. Temperature Range
c
Copy code
#include <stdio.h>
int main() {
    int temp;
    printf("Enter temperature: ");
    scanf("%d", &temp);
    switch(temp / 10) {
        case 0: case 1: printf("Cold\n"); break;
        case 2: case 3: printf("Warm\n"); break;
        case 4: case 5: printf("Hot\n"); break;
        default: printf("Very Hot\n");
    }
    return 0;
}
🧮 10. Even or Odd
c
Copy code
#include <stdio.h>
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    switch(n % 2) {
        case 0: printf("Even\n"); break;
        case 1: printf("Odd\n"); break;
    }
    return 0;
}
💧 11. Water Level Indicator
c
Copy code
#include <stdio.h>
int main() {
    int level;
    printf("Enter level (1=Low, 2=Medium, 3=High, 4=Overflow): ");
    scanf("%d", &level);
    switch(level) {
        case 1: printf("Need to fill.\n"); break;
        case 2: printf("Stable level.\n"); break;
        case 3: printf("Near full.\n"); break;
        case 4: printf("Overflow! Stop motor.\n"); break;
        default: printf("Invalid input.\n");
    }
    return 0;
}
🎮 12. Game Level
c
Copy code
#include <stdio.h>
int main() {
    int level;
    printf("Enter level (1=Easy, 2=Medium, 3=Hard): ");
    scanf("%d", &level);
    switch(level) {
        case 1: printf("You chose Easy mode.\n"); break;
        case 2: printf("You chose Medium mode.\n"); break;
        case 3: printf("You chose Hard mode.\n"); break;
        default: printf("Invalid level.\n");
    }
    return 0;
}
💡 13. Bulb State
c
Copy code
#include <stdio.h>
int main() {
    int state;
    printf("Enter bulb state (1=ON, 0=OFF): ");
    scanf("%d", &state);
    switch(state) {
        case 1: printf("The bulb is ON.\n"); break;
        case 0: printf("The bulb is OFF.\n"); break;
        default: printf("Invalid input.\n");
    }
    return 0;
}
🚗 14. Gear System
c
Copy code
#include <stdio.h>
int main() {
    int gear;
    printf("Enter gear (0=Neutral, 1–5=Drive): ");
    scanf("%d", &gear);
    switch(gear) {
        case 0: printf("Neutral\n"); break;
        case 1: printf("First Gear\n"); break;
        case 2: printf("Second Gear\n"); break;
        case 3: printf("Third Gear\n"); break;
        case 4: printf("Fourth Gear\n"); break;
        case 5: printf("Fifth Gear\n"); break;
        default: printf("Invalid gear\n");
    }
    return 0;
}
🧭 15. Direction Indicator
c
Copy code
#include <stdio.h>
int main() {
    char dir;
    printf("Enter direction (N, S, E, W): ");
    scanf(" %c", &dir);
    switch(dir) {
        case 'N': printf("Moving North\n"); break;
        case 'S': printf("Moving South\n"); break;
        case 'E': printf("Moving East\n"); break;
        case 'W': printf("Moving West\n"); break;
        default: printf("Invalid direction\n");
    }
    return 0;
}
🔢 16. Number to Word (1–5)
c
Copy code
#include <stdio.h>
int main() {
    int n;
    printf("Enter number (1-5): ");
    scanf("%d", &n);
    switch(n) {
        case 1: printf("One\n"); break;
        case 2: printf("Two\n"); break;
        case 3: printf("Three\n"); break;
        case 4: printf("Four\n"); break;
        case 5: printf("Five\n"); break;
        default: printf("Out of range\n");
    }
    return 0;
}
💻 17. System Mode
c
Copy code
#include <stdio.h>
int main() {
    int sys;
    printf("Enter system mode (1=Startup, 2=Run, 3=Shutdown): ");
    scanf("%d", &sys);
    switch(sys) {
        case 1: printf("System Starting...\n"); break;
        case 2: printf("System Running...\n"); break;
        case 3: printf("System Shutting Down...\n"); break;
        default: printf("Invalid mode.\n");
    }
    return 0;
}
🧍 18. Human Age Group
c
Copy code
#include <stdio.h>
int main() {
    int age;
    printf("Enter age: ");
    scanf("%d", &age);
    switch(age / 10) {
        case 0: case 1: printf("Child\n"); break;
        case 2: printf("Young Adult\n"); break;
        case 3: case 4: printf("Adult\n"); break;
        default: printf("Senior Citizen\n");
    }
    return 0;
}
🧾 19. Menu Selection
c
Copy code
#include <stdio.h>
int main() {
    int choice;
    printf("1.Pizza 2.Burger 3.Pasta 4.Exit\nEnter choice: ");
    scanf("%d", &choice);
    switch(choice) {
        case 1: printf("You ordered Pizza.\n"); break;
        case 2: printf("You ordered Burger.\n"); break;
        case 3: printf("You ordered Pasta.\n"); break;
        case 4: printf("Exiting...\n"); break;
        default: printf("Invalid choice.\n");
    }
    return 0;
}
🕹️ 20. Remote Control
c
Copy code
#include <stdio.h>
int main() {
    int button;
    printf("Press (1=Power, 2=Volume+, 3=Volume-, 4=Channel+): ");
    scanf("%d", &button);
    switch(button) {
        case 1: printf("Power toggled.\n"); break;
        case 2: printf("Volume increased.\n"); break;
        case 3: printf("Volume decreased.\n"); break;
        case 4: printf("Next channel.\n"); break;
        default: printf("Invalid button.\n");
    }
    return 0;
}












