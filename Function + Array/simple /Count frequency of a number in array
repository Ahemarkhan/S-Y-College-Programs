#include <stdio.h>
int frequency(int a[], int n, int key){
    int count=0;
    for(int i=0;i<n;i++)
        if(a[i]==key) count++;
    return count;
}
int main(){
    int a[8]={1,2,2,3,2,4,5,2};
    printf("Frequency = %d", frequency(a,8,2));
    return 0;
}
