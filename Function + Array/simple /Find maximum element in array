#include <stdio.h>
int maxInArray(int a[], int n){
    int max=a[0];
    for(int i=1;i<n;i++)
        if(a[i]>max) max=a[i];
    return max;
}
int main(){
    int a[5]={10,30,20,50,40};
    printf("Max = %d", maxInArray(a,5));
    return 0;
}
