#include <stdio.h>
float average(int a[], int n){
    int sum=0;
    for(int i=0;i<n;i++) sum+=a[i];
    return (float)sum/n;
}
int main(){
    int a[5]={10,20,30,40,50};
    printf("Average = %.2f", average(a,5));
    return 0;
}
