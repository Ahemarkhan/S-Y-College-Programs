#include <stdio.h>
void swapEnds(int a[], int n){
    int temp=a[0];
    a[0]=a[n-1];
    a[n-1]=temp;
}
int main(){
    int a[5]={10,20,30,40,50};
    swapEnds(a,5);
    for(int i=0;i<5;i++) printf("%d ", a[i]);
    return 0;
}
