#include <stdio.h>
void unique(int a[], int n){
    int i,j,count;
    for(i=0;i<n;i++){
        count=0;
        for(j=0;j<n;j++)
            if(a[i]==a[j] && i!=j)
                count++;
        if(count==0)
            printf("%d ",a[i]);
    }
}
int main(){
    int n,a[50];
    scanf("%d",&n);
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    unique(a,n);
    return 0;
}
