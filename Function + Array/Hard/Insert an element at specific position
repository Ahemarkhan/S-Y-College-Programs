#include <stdio.h>
void insert(int a[], int *n, int pos, int val){
    for(int i=*n;i>pos;i--)
        a[i]=a[i-1];
    a[pos]=val;
    (*n)++;
}
int main(){
    int n,a[50],pos,val;
    scanf("%d",&n);
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    scanf("%d%d",&pos,&val);
    insert(a,&n,pos,val);
    for(int i=0;i<n;i++) printf("%d ",a[i]);
    return 0;
}
