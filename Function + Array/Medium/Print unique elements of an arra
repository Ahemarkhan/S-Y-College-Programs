#include <stdio.h>
void unique(int a[], int n){
    for(int i=0;i<n;i++){
        int count=0;
        for(int j=0;j<n;j++)
            if(a[i]==a[j]) count++;
        if(count==1) printf("%d ",a[i]);
    }
}
int main(){
    int a[7]={1,2,2,3,4,4,5};
    unique(a,7);
    return 0;
}
