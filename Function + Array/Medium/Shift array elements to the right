#include <stdio.h>
void rightShift(int a[], int n){
    int last=a[n-1];
    for(int i=n-1;i>0;i--)
        a[i]=a[i-1];
    a[0]=last;
}
int main(){
    int a[5]={1,2,3,4,5};
    rightShift(a,5);
    for(int i=0;i<5;i++) printf("%d ",a[i]);
    return 0;
}
