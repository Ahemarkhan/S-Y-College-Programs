#include <stdio.h>
void leftShift(int a[], int n){
    int first=a[0];
    for(int i=0;i<n-1;i++)
        a[i]=a[i+1];
    a[n-1]=first;
}
int main(){
    int a[5]={1,2,3,4,5};
    leftShift(a,5);
    for(int i=0;i<5;i++) printf("%d ",a[i]);
    return 0;
}
