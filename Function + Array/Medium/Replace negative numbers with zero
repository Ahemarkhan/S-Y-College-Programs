#include <stdio.h>
void replaceNeg(int a[], int n){
    for(int i=0;i<n;i++)
        if(a[i]<0) a[i]=0;
}
int main(){
    int a[6]={1,-2,3,-4,5,-6};
    replaceNeg(a,6);
    for(int i=0;i<6;i++) printf("%d ",a[i]);
    return 0;
}
