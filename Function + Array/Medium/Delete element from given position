#include <stdio.h>
void deleteAt(int a[], int *n, int pos){
    for(int i=pos;i<*n-1;i++)
        a[i]=a[i+1];
    (*n)--;
}
int main(){
    int a[6]={10,20,30,40,50,60}, n=6;
    deleteAt(a,&n,2);
    for(int i=0;i<n;i++) printf("%d ",a[i]);
    return 0;
}
