#include <stdio.h>
int indexOfMax(int a[], int n){
    int max=a[0], idx=0;
    for(int i=1;i<n;i++)
        if(a[i]>max){ max=a[i]; idx=i; }
    return idx;
}
int main(){
    int a[5]={10,50,30,20,40};
    printf("Index of max = %d", indexOfMax(a,5));
    return 0;
}
