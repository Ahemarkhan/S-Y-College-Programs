#include <stdio.h>
float avgEven(int a[], int n){
    int sum=0, c=0;
    for(int i=0;i<n;i++)
        if(a[i]%2==0){ sum+=a[i]; c++; }
    return (float)sum/c;
}
int main(){
    int a[6]={1,2,3,4,5,6};
    printf("Average of even = %.2f", avgEven(a,6));
    return 0;
}
