#include <stdio.h>
void insertAt(int a[], int *n, int pos, int val){
    for(int i=*n;i>pos;i--)
        a[i]=a[i-1];
    a[pos]=val;
    (*n)++;
}
int main(){
    int a[10]={1,2,3,4,5}, n=5;
    insertAt(a,&n,2,99);
    for(int i=0;i<n;i++) printf("%d ",a[i]);
    return 0;
}
