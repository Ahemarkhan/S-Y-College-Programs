#include <stdio.h>

struct Book {
    char title[30];
    float price;
};

void display(struct Book *b) {
    printf("Book: %s | Price: %.2f\n", b->title, b->price);
}

int main() {
    struct Book b1 = {"C Programming", 399.50};
    display(&b1);
    return 0;
}
