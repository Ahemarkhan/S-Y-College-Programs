#include <stdio.h>

struct Student {
    int roll;
    float marks;
};

int main() {
    struct Student s[3] = {{1,60},{2,70},{3,80}};
    float sum=0;
    for(int i=0;i<3;i++)
        sum += s[i].marks;
    printf("Average Marks = %.2f\n", sum/3);
    return 0;
}
