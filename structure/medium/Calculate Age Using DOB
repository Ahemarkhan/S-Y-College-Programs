#include <stdio.h>

struct Date {
    int day, month, year;
};

int main() {
    struct Date dob = {5,11,2005};
    int currentYear = 2025;
    printf("Age: %d years\n", currentYear - dob.year);
    return 0;
}
