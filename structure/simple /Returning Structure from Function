#include <stdio.h>

struct Point {
    int x, y;
};

struct Point createPoint(int a, int b) {
    struct Point p;
    p.x = a;
    p.y = b;
    return p;
}

int main() {
    struct Point p1 = createPoint(5, 10);
    printf("Point: (%d, %d)\n", p1.x, p1.y);
    return 0;
}
