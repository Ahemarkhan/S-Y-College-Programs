#include <stdio.h>

struct Book {
    char title[50];
    int pages;
};

int main() {
    struct Book b1 = {"C Programming", 350};
    struct Book *ptr = &b1;
    printf("Book: %s, Pages: %d\n", ptr->title, ptr->pages);
    return 0;
}
