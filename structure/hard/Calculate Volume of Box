#include <stdio.h>

struct Box {
    float length, width, height;
};

float volume(struct Box b) {
    return b.length * b.width * b.height;
}

int main() {
    struct Box b1 = {4.5, 3.2, 2.0};
    printf("Volume = %.2f\n", volume(b1));
    return 0;
}
