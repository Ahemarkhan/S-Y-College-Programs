#include <stdio.h>

struct Time {
    int h, m, s;
};

int main() {
    struct Time t1={2,45,10}, t2={3,15,5};
    if(t1.h==t2.h && t1.m==t2.m && t1.s==t2.s)
        printf("Times are equal");
    else
        printf("Times are different");
    return 0;
}
