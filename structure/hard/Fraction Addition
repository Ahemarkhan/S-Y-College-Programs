#include <stdio.h>

struct Fraction {
    int num, den;
};

int main() {
    struct Fraction f1={1,2}, f2={1,3}, res;
    res.num = f1.num*f2.den + f2.num*f1.den;
    res.den = f1.den*f2.den;
    printf("Sum = %d/%d\n", res.num, res.den);
    return 0;
}
