#include <stdio.h>

struct Point {
    int x, y;
};

int main() {
    struct Point p1={3,4}, p2={7,9}, temp;
    temp=p1;
    p1=p2;
    p2=temp;
    printf("After Swap: P1(%d,%d) P2(%d,%d)\n", p1.x,p1.y,p2.x,p2.y);
    return 0;
}
