#include <stdio.h>

struct Student {
    char name[20];
    int marks[3];
};

int main() {
    struct Student s = {"Ravi",{40,55,60}};
    int pass=1;
    for(int i=0;i<3;i++)
        if(s.marks[i]<35)
            pass=0;
    printf("%s -> %s\n", s.name, pass?"Pass":"Fail");
    return 0;
}
