#include <stdio.h>
int main() {
    int n, a = 0, b = 1;
    printf("Enter number of terms: ");
    scanf("%d", &n);
    printf("%d %d ", a, b);
    for(int i = 3; i <= n; i++) {
        b = a + b;
        a = b - a;
        printf("%d ", b);
    }
    return 0;
}
