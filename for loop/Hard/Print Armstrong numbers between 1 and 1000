#include <stdio.h>
int main() {
    for(int i = 1; i <= 1000; i++) {
        int n = i, sum = 0, d;
        for(; n > 0; n /= 10) {
            d = n % 10;
            sum += d * d * d;
        }
        if(sum == i)
            printf("%d ", i);
    }
    return 0;
}
