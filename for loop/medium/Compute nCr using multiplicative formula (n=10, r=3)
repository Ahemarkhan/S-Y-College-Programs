#include <stdio.h>
int main() {
    int n=10, r=3;
    if(r>n) { printf("0"); return 0; }
    if(r>n-r) r = n-r;
    long res=1;
    for(int i=0;i<r;i++){
        res = res*(n-i)/(i+1);
    }
    printf("nCr = %ld", res);
    return 0;
}
