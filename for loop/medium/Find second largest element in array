#include <stdio.h>
int main() {
    int a[] = {12, 45, 2, 45, 9}, n=5;
    int max=-2147483648, smax=-2147483648;
    for(int i=0;i<n;i++){
        if(a[i]>max){ smax=max; max=a[i]; }
        else if(a[i]>smax && a[i]!=max) smax=a[i];
    }
    printf("Second largest = %d", smax);
    return 0;
}
