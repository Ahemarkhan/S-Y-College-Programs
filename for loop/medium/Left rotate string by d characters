#include <stdio.h>
#include <string.h>
int main() {
    char s[] = "rotation";
    int n = strlen(s), d = 2;
    char tmp[100];
    for(int i=0;i<n;i++) tmp[i] = s[(i+d)%n];
    tmp[n]=0;
    printf("%s", tmp);
    return 0;
}
